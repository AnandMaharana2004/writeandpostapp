<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit file</title>
    <link rel="stylesheet" href="/stylesheets/edit.css">
</head>
<body>
    <div class="wrapper">
        <h2>name : <%= post.user.name %></h2>
        <h5>update your post here</h5>
        <form action="/updatePost/<%= post._id %>" method="post" class="form">
            <textarea name="content" id="" name="content"> <%= post.content %> </textarea>
            <input type="submit" value="update your post" class="common">
        </form>
    </div>
</body>
</html>